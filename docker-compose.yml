version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: tasktracker-postgres
    environment:
      POSTGRES_DB: TaskTrackerDB
      POSTGRES_USER: tasktracker_user
      POSTGRES_PASSWORD: TaskTracker123!
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tasktracker_user -d TaskTrackerDB"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - tasktracker-network

  # API service will be added here when needed
  # api:
  #   build:
  #     context: .
  #     dockerfile: TaskTracker.API/Dockerfile
  #   container_name: tasktracker-api
  #   environment:
  #     - ASPNETCORE_ENVIRONMENT=Production
  #     - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=TaskTrackerDB;Username=tasktracker_user;Password=TaskTracker123!
  #     - ASPNETCORE_URLS=http://+:5128
  #   ports:
  #     - "5128:5128"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - tasktracker-network

  # UI service will be added here when needed
  # ui:
  #   build:
  #     context: ./task-tracker-ui
  #     dockerfile: Dockerfile
  #   container_name: tasktracker-ui
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - api
  #   networks:
  #     - tasktracker-network

volumes:
  postgres_data:
    driver: local

networks:
  tasktracker-network:
    driver: bridge
